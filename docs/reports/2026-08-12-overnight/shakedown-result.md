# Shakedown result (item 8)

## Free lane: PASS (live, isolated)
alpine_meadow_goat_pasture_terraces_bird_6dff (real 24-bird package) in
scratchpad/shakedown rig: import_authoring_from_public (canonical-package fix)
→ plan_corpus/apply_level_plan → VALID_CURRENT → resolver verified 24/24
sprites → CAS populated (26 objects) → identical-move no_op=True (approvals
preserved) → canonical readiness ready/0-missing → hydrate 24 dogs id-aligned,
obligations = [review:hitboxes, review:finalCutouts]. Zero provider spend.

## Paid regen (uk_cotswolds_3a43): PARKED — two independent blockers
1. Source package is more broken than recorded: dogs/dog_01/ absent while
   level.json references it (plus the 25-birds/20-sprites sharing). Repair
   requires choosing which sprite to duplicate = data invention = operator
   decision (morning queue).
2. CR-2 verdict NO-GO-UNTIL (docs/reports/2026-08-12-overnight/
   cr2-prepaid-audit.md): runtime attempts=1 enforcement, immutable pre-call
   manifest, code-level staging mode, executable abort rules. These are their
   own engineering tasks, not preflight paperwork.
Money spent: $0. No retries. Production untouched.

## 50-run (item 9): NOT STARTED — gate (shakedown PASS + CR-3) never opened.
