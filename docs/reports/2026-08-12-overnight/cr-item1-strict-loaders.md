I’m using the structured code-review workflow because you asked for a commit-level defect review. I’ll inspect the commit, its touched files, and the relevant loader callers, then report only actionable findings.

This is the full review path. The workflow requires independent reviewer passes; I’ll use them for loader correctness, asset-CAS commit integrity, and caller/test coverage while I verify the exact lines locally.

The first direct test command exposed an environment detail: bare `pytest` is unavailable here. I’m switching to the repository’s configured runner and continuing the semantic review; no files are being changed.

1. **P0 — Non-object pointers crash instead of failing closed.** [canonical_bird_contract.py:289](/Users/base/dev/appletolye/fabrikav2/.claude/worktrees/overnight-canonical/tools/level-editor/levelbuilder/api/canonical_bird_contract.py:289) calls `.get()` before confirming `current.json` decoded to an object. `[]` or `null` raises uncaught `AttributeError`, breaking canonical readers rather than returning `QUARANTINED_INTEGRITY`. Add a type guard and rejection tests.

2. **P0 — Existing CAS objects are trusted without verifying their bytes.** [canonical_bird_contract.py:393](/Users/base/dev/appletolye/fabrikav2/.claude/worktrees/overnight-canonical/tools/level-editor/levelbuilder/api/canonical_bird_contract.py:393) skips writing whenever the digest-named target exists. A truncated or corrupt object therefore survives a successful commit, violating the mirroring contract and losing the durable asset once its projection disappears. Verify the existing target and atomically replace or reject it; pre-seed a corrupt object in a test.

3. **P0 — Pickup preview bypasses canonical asset verification.** [routes.py:2786](/Users/base/dev/appletolye/fabrikav2/.claude/worktrees/overnight-canonical/tools/level-editor/levelbuilder/api/routes.py:2786) opens scene and restore projection paths without checking their digest/length or using the CAS resolver. During the supported pointer-commit/projection-replace crash window, it renders stale pixels inconsistent with the committed revision. Resolve both descriptors through `resolve_asset()`.

4. **P1 — A referenced asset is omitted from commit verification.** [canonical_bird_contract.py:335](/Users/base/dev/appletolye/fabrikav2/.claude/worktrees/overnight-canonical/tools/level-editor/levelbuilder/api/canonical_bird_contract.py:335) does not enumerate `birds[].activeGeneration.paintedAsset`, although that descriptor is written at [session.py:1997](/Users/base/dev/appletolye/fabrikav2/.claude/worktrees/overnight-canonical/tools/level-editor/levelbuilder/api/session.py:1997). Missing or tampered painted bytes can commit without CAS mirroring. Add it to validation/enumeration and cover its rejection.

5. **P1 — Existing revision files bypass commit-time integrity checks.** [canonical_bird_contract.py:442](/Users/base/dev/appletolye/fabrikav2/.claude/worktrees/overnight-canonical/tools/level-editor/levelbuilder/api/canonical_bird_contract.py:442) reuses an existing digest-named revision without reading it. If that file was corrupted, `commit()` reports success and installs its pointer, while the subsequent `read()` quarantines it. Digest-verify before reuse or replace atomically.

6. **P1 — Catalog snapshots still collapse semantic corruption into an empty catalog.** [public_levels.py:106](/Users/base/dev/appletolye/fabrikav2/.claude/worktrees/overnight-canonical/tools/level-editor/levelbuilder/api/public_levels.py:106) accepts any object, including `{}`, unsupported versions, mismatched `catalogRevision`, and non-list `levels`. Validate the catalog-manifest shape and requested revision. This loader has no test coverage in [test_strict_format_loaders.py:13](/Users/base/dev/appletolye/fabrikav2/.claude/worktrees/overnight-canonical/tools/level-editor/tests/test_strict_format_loaders.py:13).

7. **P1 — Archive-ledger corruption is still silently discarded.** [session.py:107](/Users/base/dev/appletolye/fabrikav2/.claude/worktrees/overnight-canonical/tools/level-editor/levelbuilder/api/session.py:107) does not validate `version` or individual session records. [session.py:126](/Users/base/dev/appletolye/fabrikav2/.claude/worktrees/overnight-canonical/tools/level-editor/levelbuilder/api/session.py:126) silently filters malformed records, hiding archive intent. Raise `FormatError` for unsupported versions and malformed entries.

8. **P1 — Strict loaders now take down graceful read-only editor lanes.** [session.py:2444](/Users/base/dev/appletolye/fabrikav2/.claude/worktrees/overnight-canonical/tools/level-editor/levelbuilder/api/session.py:2444) eagerly loads the index, catalog, and archive ledger, so any corruption makes `/sessions` at [routes.py:944](/Users/base/dev/appletolye/fabrikav2/.claude/worktrees/overnight-canonical/tools/level-editor/levelbuilder/api/routes.py:944) return 500 and hides otherwise repairable source sessions. Likewise, [sequence_activation.py:266](/Users/base/dev/appletolye/fabrikav2/.claude/worktrees/overnight-canonical/tools/level-editor/levelbuilder/api/sequence_activation.py:266) lets snapshot `FormatError` bypass its existing unavailable-catalog diagnostic. Keep mutation/publish paths fail-closed, but convert these read-only lanes to explicit corruption status/blocking diagnostics.

9. **P1 — Required rejection branches remain untested.** [test_canonical_bird_contract.py:145](/Users/base/dev/appletolye/fabrikav2/.claude/worktrees/overnight-canonical/tools/level-editor/tests/test_canonical_bird_contract.py:145) does not test pointer schema rejection, non-object pointers, revision-byte/name mismatch, or historical tamper rejection. [test_strict_format_loaders.py:30](/Users/base/dev/appletolye/fabrikav2/.claude/worktrees/overnight-canonical/tools/level-editor/tests/test_strict_format_loaders.py:30) misses catalog-manifest wrong-type/version, levels-index malformed JSON, and archive-ledger structural rejection. The objects-only fallback is also untested.

Targeted suites passed: 13 tests. The commit is not ready under the stated definition of done.

