# Screenshot Corpus

No new local post-change screenshots were captured in this worker.

Reference target:

- `../../../../games/marble_run/refs/captures/android-basegamelab/settings-from-menu.png`

Blocked local captures:

- Chromium Playwright failed before app code ran with a Mach port permission error.
- WebKit Playwright installed, but failed before app code ran with `Abort trap: 6`.

Required next proof:

- Conductor verify-device panel capture for settings on the real device, diffed against `settings-from-menu.png`.
