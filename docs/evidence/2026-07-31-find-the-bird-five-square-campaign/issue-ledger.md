# Five-square campaign issue ledger

| Area | Before | After | Verification |
|---|---|---|---|
| Campaign order | The five square levels were positions 12-16 behind eleven experimental levels. | The bundled, catalog, and level-index manifests contain exactly Hawaii, Pirate Cove, Yucatan Cenote, Sami Aurora, and Grand Bazaar, in that order. | `five-square-campaign.test.ts`; browser runtime sequence in `browser/results.json`. |
| Square hit tolerance | The legacy 3x radius produced very large, overlapping tap regions that could select a neighboring bird or scenery. | Square levels use a deterministic 1.35x radius capped at half the nearest-center distance (minus a four-pixel gap). Legacy non-square behavior is unchanged. | Red-first `hitbox-geometry.test.ts`; all 75 centers exercised in the browser. |
| Hitbox/package integrity | No single regression gate covered campaign membership, package dimensions, sprite cardinality, centered hitboxes, and asset existence. | Regression coverage requires 4096x4096, exactly 15 unique targets and sprites per level, finite in-bounds geometry, each hitbox center inside its sprite rectangle, and every referenced sprite file present. | `five-square-campaign.test.ts`; 75-entry `target-manifest.json`. |
| Browser progression | Existing checks did not complete this exact five-level sequence. | Mobile browser verification completes all 75 targets, claims each reward, advances through all five levels, and records final completion. | `browser/results.json` and 35 labeled captures. |
| Pan and zoom | The complete square and focal-point behavior were not recorded across the campaign. | Each level records minimum zoom, maximum zoom 2.5, all four pan bounds, and focal preservation with 5.50 internal-pixel maximum error. | `browser/results.json` plus edge/zoom captures. |
| Grand Bazaar composition | Acceptance required a top-down scene, not corridor perspective. | The full-resolution and runtime captures show an overhead bazaar-room composition. No regeneration was warranted. | `browser/square_grand_bazaar_flash_4k-*.png`, visually inspected. |
| Normal iPhone artifact | No current normal-player proof existed for this manifest. | A signed normal player with all three tour/harness variables unset was built, installed, launched, and remained on the level-one campaign home without an automatic tour. | `iphone-normal-updated-id/launch.png`, `launch-after-8s.png`; bundle `com.basegamelab.findthebird`; executable SHA-256 `606c92cc6c118014627c1e6e8da967529c0d2d39563e35e5029b7fd9f08e4ba4`. |
| Physical interaction | Required real taps, pan, pinch, five-level progression, and final completion on Batu's iPhone. | **Blocked, not accepted.** Signing, `pause`, and square-camera auto-win are fixed. Current XCUITest evidence reaches and captures `win`, then loses the final `fail` marker while showing stale Settings UI/AX data. Screenshots alone do not satisfy the remaining normal-player gate. | Device `00008101-000410EC3EF9001E`; `iphone-harness-camera-fixed/device.xcresult`; exact sanitized residual in `report.md`. |
