# Progression task

## Live delivery correction and activation — 2026-09-08

- [x] Confirmed the live Dog CDN already serves all 54 reviewed packages. The prior five-level conclusion omitted the CDN manifest passed by `getLevelIndex()` and applies only to bundled fallback. It is not the normal online runtime result.
- [x] Inspected the newer submission IPA at `/Users/base/store-review/find-games/ios-submission-20260906/dog/export/App.ipa`: its native Firebase project is already `find-the-dog-basegamelab`. The older Portal artifact is not the latest submission artifact. No configuration rewrite or new binary is needed to reorder the existing online catalog.
- [x] Downloaded and verified all 1,874 manifest-referenced assets against their sizes and SHA-256 hashes. All 54 live level JSON hashes match the visually ranked catalog.
- [x] Exercised the sequence resolver with the complete candidate CDN manifest: both captured project configurations resolve the exact 54-level progression with no stored sequence; the dedicated Dog empty remote sequence resolves it without diagnostics.
- [x] Published revision 11 to `ftd-levels-prod/manifest.json` under the user's explicit request to fix and serve levels. Only entry order and manifest revision/time changed. Preserved every asset, cohort and bundled flag. Archived the previous manifest to `manifests/2026-09-08-before-progression-v2.json` in the same bucket.
- [x] Verified the public origin returns HTTP 200, `Cache-Control: no-cache`, and exactly the candidate manifest. See `cdn-publish-receipt.json`, `cdn-manifest-live-readback.json`, and `cdn-assets-verification.json`.
- [x] Reordered the local editor `levels-index.json` to match live progression, preserving all 54 entry objects.
- [x] Published the corrected live-status Portal report: https://portal.basegamelab.com/media/p_44452a/01_progression.html . Hosted HTML and three JSON attachments returned HTTP 200; the report contains 54 rows and its rendered first viewport was inspected. This supersedes the older draft-only report.
- [ ] Complete physical-device gameplay inspection. Native capture of installed Dog 1.0.5 (25) showed old level 2, Coral Bommie Garden. The exact preserved build-25 archive and device artifact both contain `VITE_CDN_ENABLED=false`, explaining the fallback. The newer preserved submission archive 1.0.6 (26) has CDN enabled, a verified signature, and a profile covering this phone. It was installed successfully without uninstalling or resetting data. Before its gameplay check completed, installed-app readback changed to 1.0 (1) at a different app container; process inspection confirmed an active `ftd-ads-lifecycle` agent driving the same phone. Stopped device operations to avoid interfering. See `device-delivery-check.json`. New-order phone gameplay remains unverified. No new app build, store submission, Firebase write, Bird write or asset mutation occurred.

Online sessions fetch the new manifest at cold launch. The existing five packaged levels remain the offline/network-failure fallback. Earlier publishing-blocker statements below are historical and superseded by this complete delivery-path check.

Approved pacing: the ten easiest levels, followed by one harder level and four easier levels, repeating to the end. Challenge and recovery difficulty both rise. The last cycle contains one challenge and three recovery levels.

Sequence construction, revision 2: keep ranks 1–10. Reserve difficulty ranks 31, 32, 40, 48, 50, 51, 52, 53, 54 as the nine challenges. Fill recovery slots in ascending rank order. Exhaustive enumeration of all 82,251 selections with Waterfall and all four severe levels fixed as challenges maximizes the minimum recovery gap to 1.954545 points. Ties minimize movement from the previous draft. No selection reaches a 2-point minimum under all agreed constraints. See `optimize-progression.py` and `progression-optimization.json`.

- [x] Generate and validate the complete 54-level sequence.
- [x] Save through the existing Find the Dog editor sequence workflow and inspect compatibility diagnostics.
- [x] Publish and visually verify the progression report on Portal.

Runtime changes, if compatible and local, remain within scope; production activation and release require their separate deployment gate. The running editor on port 5196 belongs to Find the Bird and must remain untouched.

The revised draft is saved. The generic local editor dry-run still reports 54 `levelAuthoringMigrationRequired` diagnostics and one `starterPrefixMismatch`. These are local checker results, not proof that a universal Dog metadata migration is required. The earlier statement asserting that requirement is withdrawn. No metadata migration, approval-flag change, production activation or native build was performed.

Publishing authority investigation: the latest Portal IPA (1.0.3-19.2, 2026-08-27) has a verified whole-artifact hash. Its JavaScript and native Firebase settings both identify `hidden-object-base`. Authenticated Remote Config readback of that project returns `seq-5c7cd6e7723adc10`, a 74-level Bird sequence, without conditional overrides. Do not replace that payload with Dog IDs. The current Dog repo policy names `find-the-dog-basegamelab`, whose authenticated sequence readback is empty; the retained release environment still names the shared project. This build-configuration mismatch must be resolved in a Dog-specific build before publishing its sequence. The current resolver, given the exact Portal IPA manifests and both live readbacks, resolves to the old five bundled Dog levels. This is source/runtime-resolver evidence, not an installed-device capture. Latest App Store binary identity was not checked. Evidence: `firebase-sequence-readback.json`, `firebase-legacy-sequence-readback.json`, `portal-ipa-sequence-readback.json`, `runtime-sequence-readback-check.json`.

Pacing review fix: level 11 is now Mushroom Grove, with a minimum 9.425-point recovery gap. Waterfall is level 26, with a minimum 9.641-point gap. Mossy Stream Roots is level 31; recovery levels 34 and 35 remain near-ties at 1.978 and 1.955 points. These are explicitly flagged, not represented as statistically established relief. Preserve the first report and sequence in `before-review-fix-*` artifacts; Portal posts are immutable.

Verification: exact draft readback; 54 unique IDs; opening difficulty ranks 1–10; challenge positions 11 through 51 every fifth level; monotonic challenge and recovery scores; every recovery strictly lower than its preceding challenge. All four severe levels and Waterfall are challenges. Live sequence stayed identical, and runtime manifests had no git diff. This was a data-order operation with deterministic assertions, not new production code requiring new unit tests. Inline review covered completeness, duplicate IDs, score direction, final partial cycle, role allocation, and unchanged live state.

Portal post `p_e05b97` includes the HTML report, JSON sequence and newline-separated IDs. All three returned HTTP 200; hosted HTML contains 54 table rows and 9 cycles. Local desktop/mobile and hosted summary/Waterfall-cycle screenshots were inspected; mobile has no horizontal overflow. See `progression-portal-verification.json`. Local screenshots are in `/tmp/ftd-astra-ranking/progression-*.png`.

Revision 2 supersedes that post via `p_a10be8`: https://portal.basegamelab.com/media/p_a10be8/01_progression.html . HTML plus all three data attachments returned HTTP 200; each hosted data attachment exactly matches its local artifact. Hosted report contains 54 rows, nine cycles, two explicit near-tie markers and Waterfall at 26. Local desktop/mobile and hosted summary/publishing-evidence captures were inspected. `progression-v2-portal-verification.json` records the hosted checks.

Review outcome: the publishing-path finding is resolved as an evidence/authority investigation, with the shared-project build mismatch now explicitly documented as a release blocker. The two originally weak cycles are strengthened; the best feasible allocation still has two later near-ties, so player-perceived relief remains unverified. No production release is claimed. Deterministic checks reproduce the optimum and exact draft; all 108 reviewed source-image and level-JSON hashes remain unchanged. No broad test rerun was needed because no production code changed; this turn exercised the real sequence resolver with captured inputs and the finite optimizer directly.
