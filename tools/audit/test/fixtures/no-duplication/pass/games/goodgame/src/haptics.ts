// Re-uses the shared export (re-export from the package = not a duplication)
// and only declares game-specific names of its own.
export { safeImpact } from '@fabrikav2/sdk';

export function buzzOnCombo(): void {}
