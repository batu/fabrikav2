# Generated by `xcodegen generate` from project.yml at run time — only the spec
# + Swift are committed (the template). Regenerate; never commit the project.
VerifyDeviceRunner.xcodeproj/
*.xcresult/
