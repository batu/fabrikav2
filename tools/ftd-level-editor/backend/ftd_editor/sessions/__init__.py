"""Lossless, revisioned FTD authoring-session boundaries.

Import the concrete module you need (`.store`, `.model`, `.dogs`, `.gallery`,
`.routes`, `.legacy_identity`); this package intentionally re-exports nothing
so the dependency graph stays direct (U7).
"""
