# Overnight Ledger — 2026-08-14 (FTB → 100 levels + consolidation)

(Morning summary goes here at close.)

## Events

- 21:49Z RUN START (v1). Active=77. Queue=30 round-robin. Cap $60. Created below_decks_galley_bird_6b76; operator refined the ask mid-flight.
- 21:5xZ RUN RETOOLED (v2): exact-count queue — 22 scenes / 22 distinct settings (one per setting; includes 4 vista-rewrites as containment tests: uk_seaside_pier, turkey_bodrum, mexico_guanajuato, railway_sleepy_stop). Galley resumes first (paid bg reused).
- 21:5xZ Operator Q&A before departure: scrub=build-off-by-default+demo; restarts=between-levels only; bad levels=keep+flag.
- Context from the evening that this run stands on: audition pass (reading_room 24/24, $2.25), paid-singles staging (disconnect-proof), no silent model substitution (422), repair-sprites+fix-hitboxes out of the lane, delegated blessings render amber.
- 21:53Z galley_6b76 resume PARKED: author upscale step re-attempted a done upscale (skip compared bg_w to hardcoded 4096; canonical target is 2688) → server refused source_already_upscaled. FIX #1 committed (CLI-side, no restart needed): skip compares the session's own upscaleTargetLongEdge. Test added. Galley retry queued for the next between-level gap.
- 21:53Z scribes_workyard_5094 generating (level 1 of 22).
- 22:0xZ T3 CORE + STAGING DELETION committed (514 green): cutter_folder_indices is the one position→folder resolver; cutter/promotion/fit all file by the bird's dog index (== slot ordinal); promotion is a single pass; BUG-15's staged-bytes workaround deleted as structurally unnecessary. Deploys at next between-level restart gap.
- 22:01Z LEVEL 1/22 scribes_workyard_5094: PASS by eyes — 21/21 circles on birds; codex's Egyptian scene rendered fully contained (walls all sides, steles/scroll racks intact). $2.60 cumulative.
- 22:1xZ T4 committed (515 green): sprite-gaps derives from the snapshot on canonical sessions (a sprite-less bird IS a gap; the legacy walk that hid france's failed bird is now legacy-only). Deploys with T3 at the seam.
- 22:2xZ BUG-8 SCRUB built (3 tests, 518 suite green, OFF, wired nowhere): demo on reading_room — 184k mutation px reverted / 427k bird px kept; scrubbed scene visually intact (all 24 birds, no seams at review scale). Evidence: scrub-demo-before-after.png. MORNING VERDICT NEEDED before it may enter the lane.
- 22:3xZ BUG-5 ROOT FIX committed (519 green): attribution was per-wrapper and mostly absent — probe proved the contextvar machinery works; the sites were unwrapped. Now set at TWO chokepoints: session-path HTTP middleware + job-worker dispatch. Live proof expected on the first level after the seam deploy (rows must carry meta.sessionId).
- 22:10Z LEVEL 2/22 block_printing_7a65: PASS by eyes — ~22/22 circles on birds; canopy-contained. $4.65 cumulative.
- 22:10Z SEAM DEPLOY #1 (watcher): T3 (one folder namespace + staging deletion) + T4 (canonical sprite-gaps) + scrub module + upscale-resume fix now live. Level 3+ runs on consolidated code. Watcher re-armed to deploy the BUG-5 attribution chokepoints at the next seam.
- 22:4xZ SCALE INVESTIGATION (goal 3e) — finding, not a fix: fitted sprite sizes correctly track the PAINTED birds (n=79: median 1.46x hitbox diameter, p10-p90 0.98-1.99, extremes 0.78-2.25). The variance Batu hand-corrected originates at paint time (model draws birds at inconsistent scales), not in the fitter. Options for a morning decision: (a) paint-prompt size guidance ("each bird roughly the size of its magenta dot"), cheap A/B; (b) runtime pickup-display normalization (render pickup anim at normalized scale, keep in-scene sprite true to art); (c) accept variance as charm. No code changed — rescaling cutouts away from painted pixels would misalign art and sprite.
- 22:5xZ DURABLE EXTRACT-ALL committed (520 green): bulk_extract job kind; server derives detections from its own hitboxes; UI books + polls; tab closes can't orphan paid work. Deploys at next seam.
- 23:0xZ INCIDENT + ROOT CAUSE: seam deploy #1's kill hit ONE pid (`lsof|head -1`) leaving the old backend generation alive; the survivor held the jobs.worker flock, so the new backend started WORKERLESS. Every subsequent author timed out at 900s waiting on queued bg jobs — 21 sessions parked, "100 levels" reached as empty husks. Money impact: $0 wasted (bg jobs queued, not billed; spend frozen at $4.72). FIXED: full `pkill -f levelbuilder.api.server` restart discipline; single clean backend; worker claimed the queue (18 bg jobs draining, all pre-created sessions).
- 23:0xZ RECOVERY RUN launched: 22 parked sessions resume end-to-end (resume guards make paid steps free; upscale-resume fix load-bearing). Codex review to be relaunched after recovery stabilizes (first attempt collateral-killed).
- 23:3xZ CODEX REVIEW (relaunched, exit 0): no P0, seven P1. Fixed + committed this seam: (1) failed/empty VLM localization no longer stamps — pendingRelocalization stays armed (lane-threatening); (2) portrait upscale resume compares long edge; (3) attribution ContextVars now cross provider thread pools via copy_context (BUG-5 was half-deployed without this). Deferred to morning tranche: dogs[] position/slot rebuild after human deletion (P1#1), bulk_extract safeToRequeue during paid calls + dedupe race + hitbox TOCTOU (P1#4/5/6) — all human-editing/extract-all paths, untouched by tonight's lane.
- 23:3xZ SUITE: 533 passed, 1 failed — test_golden_cutout_dataset frozen-hash: public cozy_attic dog_00 sprite changed Aug 12 21:56 (operator HITL adjustment), pre-existing since before the run. Golden re-freeze is governed data → morning decision, not an overnight edit.
- 23:3xZ Seam deployer #2 armed (FULL pkill discipline, worker-lock check) — fires at next recovery DONE.
- 03:2xZ HARNESS REAPED both drivers (3rd reaping tonight). Relaunched disowned (nohup+disown, outside harness tree) — recovery runner + seam deployer now unkillable by session plumbing. Verified: exactly one runner, one author, no orphan racers. MORNING PROPOSAL: policy edit — long-running drivers always run disowned; tracked bg tasks are for short work only.
- 03:2xZ LEVEL kelp_gallery (aquarium_halls) DONE under old runner: 22 birds, eyes-on overlay PASS (all circles on birds, none missed; puffin circle a touch low but in-disc). Spend $7.84.
- 03:5xZ CODEX P1s 4/5/6 + 1 fixed (tranche 2): extract-all pre-spend safety flip + booking-sha guard + deterministic dedupe key; dogs[] rebuild keyed by bird id with slot ordinals preserved (rebuild_dog_registry, pure + tested). All 7 review findings now closed. Suite 536 green, 1 pre-existing golden-drift failure. Deploys at next seam.
- 03:5xZ BUG-5 LIVE-PROVEN: captains_cabin materialize singles land in the cost ledger tagged meta.sessionId + operation (middleware chokepoint). Worker-pool paths get the copy_context fix at next seam.
- 03:5xZ Note: author step report prints "radius 30" for auto-hitboxes but final hitboxes are uniform r=57 (T1 localization replaces radii) — cosmetic report lag, no gameplay impact.
- 04:1xZ LEVEL captains_cabin (below_decks) DONE: 22 hitboxes, eyes-on overlay — all 22 circles on real birds. FLAG: the owl on the top-left chest is painted but undetected/uncircled (visible untappable bird = de facto decoy). Doctrine accepts count drift; flagged for Batu's morning judgment.
- 04:2xZ LEVEL galley (below_decks) DONE: 17 birds, eyes-on overlay PASS (17/17 circled, no misses). The originally mid-flight level is now finished.
- 04:2xZ SEAM #2 INCIDENT (new failure mode): pkill fired while galley's sync materialize was in flight; uvicorn graceful shutdown kept the OLD process alive (draining) holding the worker flock past the NEW backend's single claim attempt → workerless again. Recovered within minutes: waited for zero survivors, restarted; worker clean; castle_keep's author survived the blip. Both P1 fix tranches are now DEPLOYED. Structural fix to build: backend retries worker-lock acquisition instead of one attempt at startup.
- 04:3xZ STRUCTURAL FIX committed: JobWorker.start(retry_interval=5.0) — lost flock claims retry in the background until the draining sibling exits; no more workerless generations. Tested (test_worker_start_retries_until_lock_frees). Deploys at any future restart; current backend already claimed cleanly, no restart needed tonight.
- 04:4xZ LEVEL great_hall (castle_keep) DONE: 22 birds, eyes-on overlay PASS (22/22, no misses). First level authored fully on the retry-fixed backend with both P1 tranches live.
- 04:5xZ LEVEL wagon_court (circus_service_compounds) DONE: 18 birds, eyes-on PASS with flag — two circles on ambiguous round brown objects on the boardwalk (feather-textured, likely curled birds; possibly sacks). Batu judges.
- 04:5xZ automaton_assembly hard-park diagnosed: its upscale job was orphaned_unknown from the 03:13 crash (money guard refuses auto-retry). Manual review: bg still 1024, no provider artifact → esrgan retry costs cents. Requeued the job, session re-appended to the runner queue. Old orphans (6, other sessions from Jul 29–Aug 12) left untouched.
- 05:1xZ LEVEL coral_bommie_garden (coral_reef) DONE: 19 birds, eyes-on PASS with 2 flags — center wren (goggles) painted but undetected (decoy); circled hummingbird is magenta-tinted (possible magenta residue on-sprite, worth operator eyes; residue gate remains RED/advisory).
- 05:2xZ LEVEL montmartre_cafe_terrace (france) DONE: 19 birds, eyes-on PASS (19/19, no misses).
- 05:3xZ LEVEL opera_backstage (grand_interiors) DONE: 21 birds, eyes-on PASS (21/21, no misses).
