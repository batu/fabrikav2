# @fabrikav2/grapes-shell

Lane tool workspace for the GrapesJS (`dom-css`) lane of the
dual-design-frontends experiment. U1 preseeds only this manifest so the root
workspace and lockfile freeze with every accepted lane dependency; U3/U4
rebase the in-flight editor/publisher implementation into it. Lane fences:
`experiments/design-frontends/fences.json`.
