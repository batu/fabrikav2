{
  "name": "@fabrikav2/grapes-shell",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Native GrapesJS authoring and content-addressed preview for real Fabrika game UI.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run",
    "lint": "eslint .",
    "verify": "npm run typecheck && npm run test:unit && npm run lint && npm run build"
  },
  "dependencies": {
    "grapesjs": "^0.23.2"
  }
}
