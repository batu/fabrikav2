# 2026-07-12 — Contract-seam P2 remediation (card qWCv9tUo)

Remediation evidence for the conductor-narrowed P2 seams from the U1
aesthetics re-review: the owned sample item's active price CTA is replaced by
an inert disabled OWNED chip (soft accent surface, dark label), the Settings
title speaks the same display-face centered page-title grammar as Shop over a
shared header grid, the lone locked sample card spans the grid's second row
instead of leaving an orphaned cell, the menu Shop entry keeps the utility
row's geometry but carries the accent fill so it reads as its own door, the
Home exit's >=48px hit target is explicitly encoded, and the locked
progression node no longer ghosts its level numeral under the padlock art.

Frames: `menu.png`, `shop.png`, `settings.png`, `pause.png`.

Capture medium — honest scope: Chromium (Playwright) against the vite dev
server, 390x844 viewport at 2x scale, states driven through the game's window
harness `driveTo`. This is a web-canvas capture of the deterministic DOM shell,
per the card's re-scoped verification ("no device claim is required for this
upstream deterministic card"). It is NOT a device claim; on-device frames come
from the lane device runs.
