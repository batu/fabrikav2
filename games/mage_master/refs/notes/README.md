# refs/notes/

Human-written design notes, briefs-in-progress, and freeform references. Read-only
to agents (see `refs/README.md`). The shared, agreed design contract belongs in
`docs/brief.md` + `docs/decisions/`; `refs/notes/` is the human's scratch pad, not
the canonical spec.
