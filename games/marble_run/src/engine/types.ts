/** Re-export shim: engine types live in the local marble-board puzzle module. */
export { gateMouthCell } from '../puzzle/marble-board';
export type {
  Cell,
  GateDef,
  LevelDef,
  MarbleColor,
  MarbleRoutePreview,
  TapChange,
} from '../puzzle/marble-board';
