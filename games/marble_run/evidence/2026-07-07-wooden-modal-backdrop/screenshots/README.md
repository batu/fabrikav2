# Screenshot Corpus

No screenshots were captured in this worker. Playwright Chromium and WebKit both aborted under the local macOS sandbox before loading the page. The attempted target set was `before-menu-settings`, `before-inlevel-settings`, `before-win`, and `before-fail` at `405x900`.

The next visual/device stage must replace this placeholder with real device panel captures.
