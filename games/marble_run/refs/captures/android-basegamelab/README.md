# Reference captures — com.basegamelab.marblerun (authoritative, Batu-confirmed)

Pixel 6a (1080x2400 @420dpi) via ubuntu-server adb lane, 2026-07-06.
Foreground package verified via dumpsys topResumedActivity at capture.

- menu.png         — coin pill + gear (teal), wooden banner, board, centered
                     zigzag saga chain w/ sun medallion, single green LEVEL btn
- settings.png     — MODAL over dimmed menu: blue card, orange ribbon header,
                     X close, Music/SFX/Haptics green toggles, green CLOSE,
                     RESET PROGRESS link. (v2 built a full PAGE — fidelity gap.)
- level-start.png  — straight top-down board ~full width; hearts panel TL,
                     gear TR, coin pill BL, tan HINT+125 square BR
- level-mid.png    — same state seconds later (ambient anim reference)

PENDING (need playthrough or scripted solve — not reachable via blind taps):
win, fail, fail-continue offer, level-complete reward flow. Capture when the
refcap tool gains an input-script recipe or a human drives the phone.
